# Collections-Utility-Methods
Writing a program that uses the 'Collections' class utility methods to sort a list of integers in ascending and descending order also, using methods to find the minimum and maximum elements in the list.
